import Label from "./Label";
import SectionWrapper from "./SectionWrapper";

const SkillsSection = () => {
  return (
    <SectionWrapper>
      <Label text="Skills" />
    </SectionWrapper>
  );
};

export default SkillsSection;
